# InfoNeedClassifier Integration - Flow Diagram

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User Message Input                          │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     ChatEngine.send_message()                       │
│                                                                     │
│  1. Save user message to chat history                              │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
                        ┌────────────────┐
                        │ Is slash       │  Yes  ┌──────────────────┐
                        │ command?       │───────▶│ Execute command  │
                        └────────┬───────┘        └──────────────────┘
                                 │ No
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                  NEW: InfoNeedClassifier.classify()                 │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Step 1: Rule-based filtering (< 10ms)                        │ │
│  │  - Check keywords (time, latest, policy, etc.)               │ │
│  │  - Check patterns (code structure, URLs, etc.)               │ │
│  │  - Calculate signal strength                                 │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Step 2: Preliminary type determination                       │ │
│  │  - AMBIENT_STATE (time, phase, status)                       │ │
│  │  - LOCAL_DETERMINISTIC (code structure)                      │ │
│  │  - EXTERNAL_FACT_UNCERTAIN (time-sensitive, authoritative)   │ │
│  │  - LOCAL_KNOWLEDGE (general questions)                       │ │
│  │  - OPINION (subjective questions)                            │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Step 3: LLM self-assessment (conditional, 500-2000ms)        │ │
│  │  - Only if signal strength < 0.5 AND type is uncertain      │ │
│  │  - Ask LLM: "Will answer be wrong in 24h?"                  │ │
│  │  - Get confidence level: HIGH | MEDIUM | LOW                │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Step 4: Decision matrix lookup                               │ │
│  │  - Combine type + confidence → action                        │ │
│  │  - LOCAL_CAPABILITY | REQUIRE_COMM | SUGGEST_COMM |          │ │
│  │    DIRECT_ANSWER                                             │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
                        ┌────────────────┐
                        │ Route based on │
                        │ decision       │
                        └────────┬───────┘
                                 │
                 ┌───────────────┼───────────────┬─────────────────┐
                 │               │               │                 │
                 ▼               ▼               ▼                 ▼
      ┌──────────────┐  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐
      │LOCAL_        │  │REQUIRE_     │  │SUGGEST_      │  │DIRECT_      │
      │CAPABILITY    │  │COMM         │  │COMM          │  │ANSWER       │
      └──────┬───────┘  └──────┬──────┘  └──────┬───────┘  └──────┬──────┘
             │                 │                 │                 │
             ▼                 ▼                 ▼                 ▼
┌─────────────────┐  ┌─────────────────┐  ┌────────────────┐  ┌──────────┐
│_handle_ambient_ │  │_handle_external_│  │_handle_with_   │  │Continue  │
│state()          │  │info_need()      │  │comm_suggestion │  │normal    │
│                 │  │                 │  │()              │  │flow      │
│- Get system     │  │- Check phase    │  │- Build context │  │          │
│  state (time,   │  │- Planning? →    │  │- Invoke LLM    │  │          │
│  phase, etc.)   │  │  block + warn   │  │- Add disclaimer│  │          │
│- No LLM         │  │- Suggest /comm  │  │- Suggest /comm │  │          │
│- Fast response  │  │- No LLM         │  │- Full answer   │  │          │
└────────┬────────┘  └────────┬────────┘  └────────┬───────┘  └─────┬────┘
         │                    │                     │                │
         │                    │                     │                ▼
         │                    │                     │         ┌─────────────┐
         │                    │                     │         │Build context│
         │                    │                     │         └──────┬──────┘
         │                    │                     │                │
         │                    │                     │                ▼
         │                    │                     │         ┌─────────────┐
         │                    │                     │         │Invoke model │
         │                    │                     │         └──────┬──────┘
         │                    │                     │                │
         └────────────────────┴─────────────────────┴────────────────┘
                                     │
                                     ▼
                            ┌────────────────┐
                            │ Save assistant │
                            │ message with   │
                            │ metadata       │
                            └────────┬───────┘
                                     │
                                     ▼
                            ┌────────────────┐
                            │ Return response│
                            │ to user        │
                            └────────────────┘
```

## Decision Matrix Detail

```
┌─────────────────────────┬────────────┬──────────────────────────┐
│ Info Need Type          │ Confidence │ Decision Action          │
├─────────────────────────┼────────────┼──────────────────────────┤
│ LOCAL_DETERMINISTIC     │ Any        │ LOCAL_CAPABILITY         │
│ AMBIENT_STATE           │ Any        │ LOCAL_CAPABILITY         │
│ LOCAL_KNOWLEDGE         │ High       │ DIRECT_ANSWER            │
│ LOCAL_KNOWLEDGE         │ Medium     │ DIRECT_ANSWER            │
│ LOCAL_KNOWLEDGE         │ Low        │ SUGGEST_COMM             │
│ EXTERNAL_FACT_UNCERTAIN │ Any        │ REQUIRE_COMM             │
│ OPINION                 │ High       │ DIRECT_ANSWER            │
│ OPINION                 │ Medium     │ SUGGEST_COMM             │
│ OPINION                 │ Low        │ REQUIRE_COMM             │
└─────────────────────────┴────────────┴──────────────────────────┘
```

## Handler Method Behaviors

### _handle_ambient_state()

```
User: "What time is it?"
     │
     ▼
┌─────────────────────────────┐
│ Detect query type           │
│ - time → datetime.now()     │
│ - phase → session.metadata  │
│ - status → system info      │
└──────────────┬──────────────┘
               │
               ▼ (< 1ms)
┌─────────────────────────────┐
│ Format response             │
│ "Current time: 13:45:23"    │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Save with metadata          │
│ classification: local_cap   │
└──────────────┬──────────────┘
               │
               ▼
         Return to user
```

### _handle_external_info_need()

```
User: "What is the latest AI policy?"
     │
     ▼
┌─────────────────────────────┐
│ Check execution_phase       │
└──────────────┬──────────────┘
               │
         ┌─────┴─────┐
         │           │
    planning    execution
         │           │
         ▼           ▼
┌─────────────┐ ┌──────────────────┐
│ Block + warn│ │ Suggest /comm    │
│ "Switch to  │ │ "/comm search    │
│ execution"  │ │  <query>"        │
└──────┬──────┘ └────────┬─────────┘
       │                 │
       └────────┬────────┘
                │
                ▼
┌─────────────────────────────────┐
│ Save message                    │
│ - ExternalInfoDeclaration       │
│ - classification: require_comm  │
│ - No LLM answer provided        │
└────────────────┬────────────────┘
                 │
                 ▼
          Return to user
```

### _handle_with_comm_suggestion()

```
User: "What is Python 3.13?"
     │
     ▼
┌─────────────────────────────┐
│ Build context               │
│ (RAG + memory)              │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Invoke LLM                  │
│ Generate full answer        │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Append disclaimer           │
│ "💡 This may not be latest" │
│ "Use: /comm search ..."     │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Save message                │
│ classification: suggest_comm│
└──────────────┬──────────────┘
               │
               ▼
          Return to user
```

## Classification Signal Strength Calculation

```
┌─────────────────────────────────────────────────────────────────┐
│ Signal Strength = Σ(weighted keyword/pattern matches)          │
│                                                                 │
│ Weights:                                                        │
│ - AMBIENT_STATE keywords:      +0.4  (strong)                  │
│ - CODE_STRUCTURE patterns:     +0.3  (strong)                  │
│ - TIME_SENSITIVE keywords:     +0.2  (medium)                  │
│ - AUTHORITATIVE keywords:      +0.2  (medium)                  │
│ - OPINION indicators:          +0.1  (weak)                    │
│ - Multiple matches (3+):       +0.1  (bonus)                   │
│ - Multiple matches (5+):       +0.2  (bonus)                   │
│                                                                 │
│ Clamp: 0.0 ≤ strength ≤ 1.0                                    │
└─────────────────────────────────────────────────────────────────┘

Examples:

Message: "What time is it?"
  - AMBIENT_STATE (time): +0.4
  Signal strength: 0.4 → AMBIENT_STATE → LOCAL_CAPABILITY

Message: "What is the latest AI policy?"
  - TIME_SENSITIVE (latest): +0.2
  - AUTHORITATIVE (policy): +0.2
  - Multiple (2): +0.1
  Signal strength: 0.5 → EXTERNAL_FACT_UNCERTAIN → REQUIRE_COMM

Message: "Where is ChatEngine class?"
  - CODE_STRUCTURE (class): +0.3
  Signal strength: 0.3 → LOCAL_DETERMINISTIC → LOCAL_CAPABILITY
```

## Performance Characteristics

```
┌──────────────────────────────────────────────────────────────────┐
│                    Classification Path                           │
└──────────────────────────────────────────────────────────────────┘

Fast Path (80% of queries):
┌──────────────┐  < 10ms   ┌──────────────┐
│ Rule-based   │ ────────▶ │ Decision     │
│ filtering    │           │ matrix       │
└──────────────┘           └──────────────┘

Slow Path (20% of queries):
┌──────────────┐  < 10ms   ┌──────────────┐  500-2000ms  ┌──────────┐
│ Rule-based   │ ────────▶ │ LLM self-    │ ──────────▶  │ Decision │
│ filtering    │           │ assessment   │              │ matrix   │
└──────────────┘           └──────────────┘              └──────────┘

Total Classification Time:
- 80%: < 10ms (rule-based only)
- 20%: 500-2000ms (with LLM evaluation)
- Average: ~100ms

Response Time by Classification:
- LOCAL_CAPABILITY: < 10ms (no LLM)
- REQUIRE_COMM: < 50ms (no LLM, formatting only)
- SUGGEST_COMM: 1000-3000ms (LLM invocation + formatting)
- DIRECT_ANSWER: 1000-3000ms (LLM invocation)
```

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                     Classification Attempt                       │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
               Success            Failure
                    │                 │
                    │                 ▼
                    │       ┌───────────────────┐
                    │       │ Log warning       │
                    │       │ "Classification   │
                    │       │  failed, falling  │
                    │       │  back to direct   │
                    │       │  answer"          │
                    │       └────────┬──────────┘
                    │                │
                    │                ▼
                    │       ┌───────────────────┐
                    │       │ Continue normal   │
                    │       │ flow (build       │
                    │       │ context + invoke  │
                    │       │ model)            │
                    │       └────────┬──────────┘
                    │                │
                    └────────────────┴───────▶
                             │
                             ▼
                    User gets response
              (classification failure is invisible)
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                       ChatEngine Components                      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Slash Command Router                                     │  │
│  │ - Built-in commands (/help, /model, /comm, etc.)        │  │
│  │ - Extension commands (from registry)                     │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │ Not a slash command                   │
│                         ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ NEW: InfoNeedClassifier                                  │  │
│  │ - Classify message                                       │  │
│  │ - Route to handler OR continue                           │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │ DIRECT_ANSWER or handler returned     │
│                         ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Context Builder                                          │  │
│  │ - Assemble conversation history                          │  │
│  │ - RAG (retrieve relevant chunks)                         │  │
│  │ - Memory (retrieve facts)                                │  │
│  │ - Budget management                                      │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Model Router                                             │  │
│  │ - Select provider (ollama, openai, etc.)                │  │
│  │ - Select model                                           │  │
│  │ - Check health                                           │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Model Adapter                                            │  │
│  │ - Invoke LLM                                             │  │
│  │ - Stream or non-stream                                   │  │
│  │ - Return response                                        │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Chat Service                                             │  │
│  │ - Save assistant message                                 │  │
│  │ - Update metadata                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

Key: NEW component in green shows integration point
     Existing components unchanged
```

## Summary

The InfoNeedClassifier integration adds **intelligent routing** to the ChatEngine:

1. **Fast path** for ambient state queries (< 10ms)
2. **Smart detection** of external info needs (blocks/suggests)
3. **Phase gate enforcement** (planning vs execution)
4. **Graceful degradation** (classification failure → normal flow)
5. **Zero breaking changes** (all existing features work)

The integration is **minimal** (one new step in flow) but **powerful** (avoids LLM for ~20% of queries, prevents outdated answers).

---

**Flow Complexity**: Low (1 new decision point)
**Performance Impact**: Minimal (< 10ms for 80% of queries)
**Breaking Changes**: Zero
**Benefits**: High (intelligent routing, phase enforcement, faster responses)
