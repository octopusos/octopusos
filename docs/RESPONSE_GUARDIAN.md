# Response Guardian - 响应卫士机制

## 📋 概述

**Response Guardian (响应卫士)** 是 AgentOS 的一个关键执行阶段保障机制,用于防止系统在具备外部能力的情况下,仍然返回"我无法访问实时数据"这类传统 LLM 兜底话术。

### 核心问题

在引入 AutoComm (自动通信) 功能后,系统**实际上已经具备了查询实时天气、搜索网络等外部能力**,但在某些情况下,LLM 仍可能生成这样的响应:

```
抱歉,作为一个AI助手,我无法直接访问实时天气数据。
建议您查看 weather.com 或使用手机天气应用。
```

这种行为会导致:
1. **用户困惑**: 明明系统有能力,却说"我无法访问"
2. **能力隐藏**: 用户不知道可以使用 AutoComm 或 `/comm` 命令
3. **体验回退**: 退化为传统 ChatGPT 模式,没有利用 AgentOS 的执行能力

### 解决方案

Response Guardian 在 LLM 生成响应后、返回给用户前,进行**拦截检查**:
- **Planning Phase**: 允许所有响应(包括"我无法访问")
- **Execution Phase + AutoComm 可用**: **阻止**"我无法访问"话术,强制系统声明正确的能力使用方式

---

## 🎯 工作原理

### 1. 检测流程

```
LLM 生成响应
    ↓
Response Guardian 检查
    ↓
┌─────────────────────┐
│ Planning Phase?     │ → Yes → 允许通过 (不拦截)
└─────────────────────┘
    ↓ No (Execution Phase)
┌─────────────────────┐
│ 检测能力拒绝模式?    │ → No → 允许通过
└─────────────────────┘
    ↓ Yes (检测到"我无法访问")
┌─────────────────────┐
│ AutoComm 可用?      │ → No → 允许通过
└─────────────────────┘
    ↓ Yes
⚠️ 阻止响应 → 生成替换内容
```

### 2. 拦截模式

Guardian 检测以下中英文模式:

**中文模式**:
- `我无法.*访问.*实时`
- `我无法.*直接.*获取`
- `我无法.*查询.*当前`
- `我不能.*访问.*实时`
- `抱歉.*我.*无法.*访问`
- `作为.*AI.*我无法`

**英文模式**:
- `I cannot.*access.*real-time`
- `I cannot.*directly.*access`
- `I don't have.*access to.*current`
- `I am unable to.*access`
- `As an AI.*I cannot`

**建议模式** (如果是唯一响应则拦截):
- `建议.*查看.*weather.com`
- `建议.*使用.*手机.*天气`
- `you.*should.*check.*weather`

### 3. 替换响应

当拦截发生时,Guardian 生成替换响应:

```markdown
⚠️ **System Capability Enforcement**

The system attempted to return a 'cannot access' response,
but this is Execution Phase and external capabilities are available.

**Correct behavior**: This question requires external information
and should be handled via AutoComm or `/comm` command.

**What happened**: The LLM generated a 'cannot access real-time data' response,
which conflicts with the system's declared AutoComm capabilities.

**Next steps**:
1. Use `/comm search <query>` to get external information
2. Or ask your question again - the system will attempt AutoComm
3. If this persists, check session metadata: `auto_comm_enabled=true`

---
_This message was generated by Response Guardian to prevent capability denial
when capabilities are available._
```

---

## 🚀 使用场景

### 场景 1: 用户问天气 (Execution Phase)

**没有 Response Guardian 之前**:
```
用户: 悉尼现在天气怎么样?
系统: 抱歉,我无法访问实时天气数据。建议您查看 weather.com。
```

**有 Response Guardian 之后**:
```
用户: 悉尼现在天气怎么样?
系统: [Guardian 拦截]
      ⚠️ System Capability Enforcement
      [替换响应,引导用户使用 /comm 或触发 AutoComm]
```

或者,更好的情况是,AutoComm 直接执行:
```
用户: 悉尼现在天气怎么样?
系统: 🌤️ 根据实时查询,悉尼当前天气为晴,温度 25°C...
      [AUTOCOMM_SUCCESS 事件已记录]
```

### 场景 2: Planning Phase (不拦截)

在 Planning Phase,系统可能确实不应该执行外部操作,此时允许"我无法访问"响应:

```
用户: 悉尼现在天气怎么样?
系统: ⚠️ 这个问题需要外部信息,但当前执行阶段是 'planning'。

      要获取外部信息,你需要:
      1. 切换到 execution phase: `/phase execution`
      2. 使用通信命令: `/comm search <query>`
```

这是**正确的行为**,Guardian 不会拦截。

### 场景 3: AutoComm 被禁用

如果 `auto_comm_enabled=False`,Guardian 允许拒绝响应:

```
用户: 悉尼现在天气怎么样?
系统: 抱歉,我无法访问实时天气数据 (AutoComm 已禁用)。
      [Guardian: 允许通过,因为 auto_comm_enabled=False]
```

---

## 🔧 配置和集成

### 1. ChatEngine 集成

Response Guardian 已集成到 `ChatEngine._invoke_model()` 和 `_stream_response()`:

```python
# 在 _invoke_model 中
if session_id:
    session = self.chat_service.get_session(session_id)
    final_response, guardian_metadata = check_response_with_guardian(
        response_content=response,
        session_metadata=session.metadata,
        classification=None
    )

    # 合并 guardian metadata
    if guardian_metadata:
        metadata = metadata or {}
        metadata['response_guardian'] = guardian_metadata

    return final_response, metadata
```

### 2. 便捷函数

使用 `check_response_with_guardian` 便捷函数:

```python
from agentos.core.chat.response_guardian import check_response_with_guardian

final_response, guardian_metadata = check_response_with_guardian(
    response_content=llm_response,
    session_metadata=session.metadata,
    classification=classification_result  # 可选
)

# final_response: 经过 Guardian 检查后的最终响应
# guardian_metadata: Guardian 决策元数据
```

### 3. 配置选项

```python
from agentos.core.chat.response_guardian import ResponseGuardian

guardian = ResponseGuardian(config={
    'enabled': True,        # 是否启用 Guardian
    'strict_mode': True     # 严格模式 (保留,未来扩展)
})
```

### 4. Session Metadata 要求

Guardian 依赖以下 session metadata:

```python
session.metadata = {
    'execution_phase': 'execution',  # 'planning' or 'execution'
    'auto_comm_enabled': True        # AutoComm 是否可用
}
```

---

## 📊 可观测性

### 1. 日志事件

当 Guardian 拦截响应时,会记录结构化日志:

```python
logger.warning(
    "Response Guardian BLOCKED capability denial in Execution Phase",
    extra={
        "event": "RESPONSE_GUARDIAN_BLOCK",
        "execution_phase": "execution",
        "matched_pattern": "我无法.*访问.*实时",
        "response_preview": response_content[:200]
    }
)
```

### 2. Metadata 追踪

Guardian 决策会附加到响应 metadata:

```python
{
    "response_guardian": {
        "execution_phase": "execution",
        "auto_comm_enabled": True,
        "matched_pattern": "我无法.*访问.*实时",
        "guardian_action": "blocked"
    }
}
```

### 3. 查询 Guardian 活动

```python
# 查询日志中的 Guardian 事件
grep "RESPONSE_GUARDIAN_BLOCK" logs/agentos.log

# 或使用结构化日志查询
SELECT * FROM audit_events
WHERE metadata->>'event' = 'RESPONSE_GUARDIAN_BLOCK'
```

---

## ✅ 测试

### 运行测试

```bash
pytest tests/unit/core/chat/test_response_guardian.py -v
```

### 关键测试用例

1. **Planning Phase 允许所有响应**
2. **Execution Phase 拦截中文能力拒绝**
3. **Execution Phase 拦截英文能力拒绝**
4. **Execution Phase 允许正确使用能力的响应**
5. **AutoComm 禁用时允许拒绝响应**
6. **Guardian 禁用时允许所有响应**
7. **替换响应质量检查**
8. **Metadata 追踪完整性**

---

## 🎓 最佳实践

### 1. 何时使用 Response Guardian

✅ **应该使用**:
- Execution Phase 且 AutoComm 可用
- 用户期望系统具备外部能力
- 需要防止能力拒绝混淆用户

❌ **不应该使用**:
- Planning Phase (允许拒绝)
- AutoComm 明确禁用
- 系统确实没有该能力时

### 2. 与 AutoComm 的关系

Response Guardian 是 **AutoComm 的补充保障机制**:

```
┌──────────────────────────────────────────────────────┐
│ InfoNeedClassifier                                    │
│  → 分类: REQUIRE_COMM                                 │
└──────────────────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────────────────┐
│ AutoCommPolicy.decide()                              │
│  → 决策: allowed=True (天气白名单)                     │
└──────────────────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────────────────┐
│ AutoComm 执行                                         │
│  → 触发 AUTOCOMM_ATTEMPT                             │
│  → 执行外部查询                                        │
│  → 记录 AUTOCOMM_SUCCESS                             │
└──────────────────────────────────────────────────────┘
```

**如果 AutoComm 路径失败/未触发,导致 LLM 生成兜底响应**:

```
┌──────────────────────────────────────────────────────┐
│ LLM 生成: "我无法访问实时数据"                          │
└──────────────────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────────────────┐
│ Response Guardian 拦截                                │
│  → 检测能力拒绝模式                                    │
│  → 生成替换响应                                        │
│  → 记录 RESPONSE_GUARDIAN_BLOCK                      │
└──────────────────────────────────────────────────────┘
```

### 3. 与 InfoNeedClassifier 的协同

Guardian 可以接收 classification 结果,生成更精准的替换响应:

```python
final_response, guardian_metadata = check_response_with_guardian(
    response_content=response,
    session_metadata=session.metadata,
    classification=classification_result  # 传入分类结果
)
```

如果 classification 显示 `REQUIRE_COMM`,替换响应会明确说明:

```
**Correct behavior**: This question requires external information
and should be handled via AutoComm or `/comm` command.
```

---

## 🔍 故障排查

### 问题 1: Guardian 没有拦截"我无法访问"响应

**检查清单**:
1. 确认 execution_phase = 'execution'
2. 确认 auto_comm_enabled = True
3. 确认 Guardian 已启用
4. 检查响应文本是否匹配拦截模式

**调试**:
```python
from agentos.core.chat.response_guardian import ResponseGuardian

guardian = ResponseGuardian()
denial_found, pattern = guardian._detect_capability_denial(response_text)
print(f"Denial found: {denial_found}, Pattern: {pattern}")
```

### 问题 2: Guardian 过度拦截

**症状**: 正常响应也被拦截

**解决**:
1. 检查拦截模式是否过于宽泛
2. 查看 logs 中的 matched_pattern
3. 如需调整,修改 `CAPABILITY_DENIAL_PATTERNS`

### 问题 3: 替换响应不够友好

**解决**:
自定义 `_generate_replacement_response()` 方法:

```python
def _generate_replacement_response(self, original_response, classification):
    # 自定义替换响应逻辑
    return "您的自定义提示信息..."
```

---

## 📚 相关文档

- [AutoComm Observability](./AUTOCOMM_OBSERVABILITY.md)
- [InfoNeedClassifier](./INFO_NEED_CLASSIFICATION.md)
- [Communication Service](./COMMUNICATION_SERVICE.md)
- [Time Contract](./adr/ADR-011-time-timestamp-contract.md)

---

## 🎯 总结

Response Guardian 是 AgentOS 从"传统 LLM 模式"向"执行型 Agent 系统"转变的关键保障:

- **Planning Phase**: 允许探索和建议,不强制执行
- **Execution Phase**: 强制使用声明的能力,不允许能力拒绝

这确保了:
1. **用户体验一致性**: 系统行为符合能力声明
2. **可观测性**: 所有拦截事件都有日志和 metadata
3. **渐进增强**: 可以逐步添加新的拦截模式和替换策略

---

**最后更新**: 2026-01-31
**维护者**: AgentOS Core Team
