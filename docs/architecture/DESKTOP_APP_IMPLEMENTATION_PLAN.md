# AgentOS Desktop 桌面应用实施方案

> **版本**: 1.0
> **日期**: 2026-01-30
> **状态**: 架构设计阶段
> **目标**: 将 AgentOS 打包为开箱即用的桌面应用（macOS/Windows）

---

## 📋 执行概要

### 核心目标

将 AgentOS 从"需要配置的开发工具"升级为"开箱即用的桌面产品"，消除以下心智阻碍：

- ❌ 用户不需要看到 Python
- ❌ 用户不需要手动安装 Ollama
- ❌ 用户不需要理解端口/命令行概念
- ✅ 安装后 3 步内完成首次对话

### 产品形态

```
AgentOS Desktop = Tauri Shell + AgentOS Runtime + Ollama Server + 统一更新机制
```

---

## 🏗️ 总体架构

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    AgentOS Desktop App                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Frontend (Chat UI)                       │  │
│  │  - 简化聊天界面                                        │  │
│  │  - "Open WebUI" 按钮 → 完整管理界面                  │  │
│  │  - 运行状态面板（AgentOS/Ollama/模型管理）           │  │
│  └───────────────────────────────────────────────────────┘  │
│                           ↕ IPC                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │            Tauri Backend (Rust)                       │  │
│  │  - Sidecar 生命周期管理                               │  │
│  │  - 更新检测与应用                                      │  │
│  │  - 系统资源检测                                        │  │
│  │  - 配置管理                                            │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌──────────────────────┐  ┌──────────────────────────┐     │
│  │  Sidecar #1          │  │  Sidecar #2              │     │
│  │  AgentOS Runtime     │  │  Ollama Server           │     │
│  │  (Python → Binary)   │  │  (Go Binary)             │     │
│  │  Port: 8000 (auto)   │  │  Port: 11434 (auto)      │     │
│  └──────────────────────┘  └──────────────────────────┘     │
│           ↕                          ↕                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         User Data Directory (~/.agentos)              │   │
│  │  - models/     (Ollama 模型)                          │   │
│  │  - config/     (配置文件)                             │   │
│  │  - logs/       (运行日志)                             │   │
│  │  - updates/    (下载的更新包)                         │   │
│  │  - agentos.db  (任务数据库)                           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 技术选型决策（冻结）

| 组件 | 技术选型 | 理由 |
|------|---------|------|
| **Desktop Shell** | Tauri 2.x | • Rust 性能与安全性<br>• 原生 sidecar 支持<br>• 更小的包体积（vs Electron）<br>• 跨平台签名支持 |
| **AgentOS Runtime** | Nuitka 打包 | • 单可执行文件（用户无感知 Python）<br>• 比 PyInstaller 更好的性能<br>• 支持增量更新 |
| **Ollama** | 官方二进制 sidecar | • MIT 许可证，可随包分发<br>• 通过环境变量配置<br>• 独立版本管理 |
| **更新机制** | Tauri Updater + 自定义组件更新 | • App 本体：Tauri 自动更新<br>• Runtime/Ollama：重启后替换 |
| **前端框架** | React + Vite | • 复用现有 WebUI 资源<br>• 快速开发迭代 |

---

## 📂 目录结构设计

### App 安装目录结构（冻结）

```
AgentOS.app/                          # macOS
AgentOS.exe + resources/              # Windows

└── resources/
    ├── runtime/
    │   ├── agentos-runtime(.exe)     # 打包后的 Python 运行时
    │   ├── version.json               # 版本信息
    │   └── config/                    # 内置配置模板
    ├── ollama/
    │   ├── ollama(.exe)              # Ollama 二进制
    │   └── version.json
    ├── updater/
    │   ├── manifest.json             # 更新清单
    │   └── updater-helper(.exe)      # 更新辅助进程
    └── licenses/
        ├── AgentOS-LICENSE
        ├── Ollama-LICENSE
        └── THIRD_PARTY_NOTICES
```

### 用户数据目录（不可被更新覆盖）

```
~/.agentos/                           # Linux/macOS
%USERPROFILE%/.agentos/               # Windows

├── models/                           # Ollama 模型存储
│   ├── llama3/
│   ├── codellama/
│   └── ...
├── config/
│   ├── settings.json                 # 用户配置
│   ├── providers.json                # Provider 配置
│   └── ports.json                    # 端口分配记录
├── logs/
│   ├── agentos-runtime.log
│   ├── ollama.log
│   └── desktop-app.log
├── cache/
│   └── health-check-cache.json
├── updates/
│   ├── pending/                      # 下载中的更新
│   └── staging/                      # 已下载待应用
└── agentos.db                        # 任务数据库
```

---

## 🚀 分阶段实施计划

### Phase 0: 基础设施与约束冻结（1-2 周）

**目标**: 确定不可变的运行边界，为后续开发奠定基础

#### 任务清单

| ID | 任务 | 输出物 | DoD |
|----|------|-------|-----|
| P0.1 | 目录结构设计审查 | `DIRECTORY_STRUCTURE.md` | 团队签字确认 |
| P0.2 | 版本管理协议定义 | `VERSION_PROTOCOL.md` | 包含 version.json schema |
| P0.3 | 更新边界声明 | `UPDATE_BOUNDARIES.md` | 明确可/不可更新的路径 |
| P0.4 | 端口分配策略 | `PORT_ALLOCATION.md` | 冲突检测与自动切换规则 |
| P0.5 | 用户数据迁移策略 | `DATA_MIGRATION.md` | 从现有 AgentOS 迁移方案 |

#### 关键决策点

1. **模型目录位置**：
   - 默认：`~/.agentos/models`
   - 用户可自定义（首次启动时选择）
   - 空间预检：至少 20GB 可用空间

2. **端口分配规则**：
   ```json
   {
     "agentos_runtime": {
       "default": 8000,
       "fallback_range": [8001, 8010]
     },
     "ollama_server": {
       "default": 11434,
       "fallback_range": [11435, 11444]
     }
   }
   ```

3. **禁止事项（必须冻结）**：
   - ❌ 运行中覆盖二进制
   - ❌ 在用户数据目录存放可执行文件
   - ❌ 更新时删除 models/ 或 agentos.db

---

### Phase 1: Sidecar 生命周期管理（2-3 周）

**目标**: 实现 AgentOS Runtime 和 Ollama Server 的启动、监控、停止

#### 1.1 Sidecar 状态机（冻结）

```
STOPPED → STARTING → READY → [DEGRADED] → STOPPING → STOPPED
                  ↓
               FAILED
```

#### 1.2 实现组件

| 组件 | 职责 | 技术栈 |
|------|------|--------|
| `SidecarManager` | 统一管理 sidecar 生命周期 | Rust/Tauri |
| `ProcessMonitor` | 健康检查、自动重启 | Rust + HTTP health check |
| `LogCollector` | 收集 stdout/stderr 到 AgentOS logs | Rust |
| `PortScanner` | 端口占用检测与分配 | Rust |

#### 1.3 启动流程（关键路径）

```rust
// 伪代码示例
fn startup_sequence() -> Result<(), Error> {
    // 1. 端口检测
    let agentos_port = find_available_port(8000, 8010)?;
    let ollama_port = find_available_port(11434, 11444)?;

    // 2. 启动 AgentOS Runtime
    start_sidecar("agentos-runtime", &[
        format!("--port={}", agentos_port),
        "--data-dir={}".to_string(),
    ])?;

    // 3. 等待健康检查
    wait_for_health(format!("http://127.0.0.1:{}/health", agentos_port))?;

    // 4. 启动 Ollama
    start_sidecar("ollama", &[
        "serve",
        format!("--host=127.0.0.1:{}", ollama_port),
    ])?;

    wait_for_health(format!("http://127.0.0.1:{}/api/health", ollama_port))?;

    Ok(())
}
```

#### 1.4 健康检查策略

| Sidecar | 检查方式 | 间隔 | 失败阈值 | 恢复策略 |
|---------|---------|------|---------|---------|
| AgentOS Runtime | `GET /health` | 30s | 3次 | 自动重启 |
| Ollama Server | `GET /api/health` | 30s | 3次 | 自动重启 |

#### DoD

- [ ] 两个 sidecar 可以启动并通过健康检查
- [ ] 端口冲突时自动切换到备用端口
- [ ] 所有日志进入 `~/.agentos/logs/`
- [ ] App 退出时 sidecar 正常停止（SIGTERM → 10s → SIGKILL）
- [ ] 编写单元测试：启动、停止、健康检查、自动重启

---

### Phase 2: AgentOS Runtime 打包（2-3 周）

**目标**: 将 Python 应用打包为单可执行文件

#### 2.1 打包工具选择：Nuitka

**理由**：
- ✅ 单可执行文件，无需解压
- ✅ 支持 macOS/Windows 代码签名
- ✅ 性能优于 PyInstaller（10-15%）
- ✅ 增量更新友好

#### 2.2 打包配置

```python
# nuitka-config.py
import nuitka

nuitka.build(
    main="agentos/cli/main.py",
    standalone=True,
    onefile=True,
    include_package_data=["agentos"],
    include_data_files=[
        ("agentos/webui/static", "webui/static"),
        ("agentos/webui/templates", "webui/templates"),
        ("agentos/store/migrations", "store/migrations"),
    ],
    enable_plugins=["pyside6"],  # 如果需要 Qt
    macos_create_app_bundle=False,  # 我们自己用 Tauri 管理
    windows_console_mode="disable",  # 桌面应用无控制台
)
```

#### 2.3 依赖处理

**关键依赖**：
- SQLite：使用 Python 内置版本
- FastAPI + Uvicorn：打包进 binary
- 静态资源（webui/static）：嵌入到可执行文件

**外部依赖（需运行时）**：
- Ollama：通过 sidecar 提供
- Git（可选）：检测系统安装，不强制要求

#### 2.4 跨平台测试矩阵

| 平台 | 架构 | Python 版本 | 测试负责人 |
|------|------|------------|-----------|
| macOS | x86_64 | 3.13 | TBD |
| macOS | arm64 | 3.13 | TBD |
| Windows | x86_64 | 3.13 | TBD |

#### DoD

- [ ] 打包后的 binary 可以独立运行
- [ ] 启动时间 < 3 秒
- [ ] 包含所有必要静态资源
- [ ] 在 macOS 和 Windows 上通过签名验证
- [ ] 编写自动化打包脚本

---

### Phase 3: 版本管理与热更新（3-4 周）

**目标**: 实现组件级热更新，重启后生效

#### 3.1 版本声明格式（冻结）

```json
{
  "name": "agentos-runtime",
  "version": "0.4.2",
  "build": "20260130",
  "sha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
  "platform": "macos-arm64"
}
```

#### 3.2 Update Manifest（远端）

```json
{
  "app": {
    "version": "0.4.2",
    "platforms": {
      "macos-arm64": {
        "url": "https://releases.agentos.com/app/v0.4.2/AgentOS-macos-arm64.dmg",
        "sha256": "..."
      },
      "windows-x64": {
        "url": "https://releases.agentos.com/app/v0.4.2/AgentOS-windows-x64.exe",
        "sha256": "..."
      }
    }
  },
  "components": {
    "agentos-runtime": {
      "version": "0.4.2",
      "platforms": {
        "macos-arm64": {
          "url": "https://releases.agentos.com/runtime/v0.4.2/agentos-runtime-macos-arm64",
          "sha256": "..."
        }
      }
    },
    "ollama": {
      "version": "0.5.4",
      "platforms": {
        "macos-arm64": {
          "url": "https://releases.agentos.com/ollama/v0.5.4/ollama-macos-arm64",
          "sha256": "..."
        }
      }
    }
  }
}
```

#### 3.3 更新流程（严格按步骤）

```
CHECK_UPDATE
 ↓
DOWNLOAD (into ~/.agentos/updates/pending/)
 ↓
VERIFY (sha256)
 ↓
STAGE (move to ~/.agentos/updates/staging/)
 ↓
MARK_PENDING (write pending_update.json)
 ↓
PROMPT_RESTART (UI 提示)
 ↓
USER_CLICKS_RESTART
 ↓
STOP_SIDECARS
 ↓
EXIT_APP
 ↓
UPDATER_HELPER_TAKES_OVER
 ↓
REPLACE_FILES (resources/runtime/* 和 resources/ollama/*)
 ↓
RESTART_APP
 ↓
VERIFY_NEW_VERSION
```

#### 3.4 UpdaterHelper 设计

**职责**：
- 替换 `resources/runtime/` 和 `resources/ollama/`
- 失败时回滚到 `.bak` 文件
- 写入更新日志到 `~/.agentos/logs/update.log`

**生命周期**：
```
App 退出 → Helper 启动 → 替换文件 → Helper 退出 → App 重启
```

**实现语言**：Rust（编译为独立可执行文件）

#### 3.5 回滚策略

```rust
fn apply_update() -> Result<(), Error> {
    // 1. 备份现有文件
    backup_file("resources/runtime/agentos-runtime", ".bak")?;
    backup_file("resources/ollama/ollama", ".bak")?;

    // 2. 替换文件
    match replace_files() {
        Ok(_) => {
            log_success("Update applied successfully");
            Ok(())
        },
        Err(e) => {
            log_error(&format!("Update failed: {}", e));
            // 3. 回滚
            restore_file("resources/runtime/agentos-runtime.bak")?;
            restore_file("resources/ollama/ollama.bak")?;
            Err(e)
        }
    }
}
```

#### DoD

- [ ] 更新只在重启后生效
- [ ] sha256 不匹配时拒绝更新
- [ ] 下载中断可恢复
- [ ] 更新失败自动回滚
- [ ] UI 明确显示"下载中 42%"和"准备重启"
- [ ] 用户永远不会看到"更新完成但没生效"的状态

---

### Phase 4: 模型管理与进度可视化（2-3 周）

**目标**: 在 UI 中管理 Ollama 模型，显示下载进度

#### 4.1 模型市场 UI

```
┌─────────────────────────────────────────┐
│         Model Marketplace               │
├─────────────────────────────────────────┤
│  🔍 Search: ________________  [Search]  │
│                                          │
│  ┌─────────────────────────────────┐   │
│  │ Llama 3.1 (8B)                  │   │
│  │ Fast and efficient for general  │   │
│  │ Size: 4.7 GB | License: Llama 3 │   │
│  │ [View License] [Download]       │   │
│  └─────────────────────────────────┘   │
│                                          │
│  ┌─────────────────────────────────┐   │
│  │ CodeLlama (13B)                 │   │
│  │ Specialized for code generation │   │
│  │ Size: 7.4 GB | License: Llama 2 │   │
│  │ [View License] [Download]       │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

#### 4.2 许可证确认流程

**重要**：模型下载前必须展示许可证并要求用户确认

```javascript
// 伪代码
async function downloadModel(modelId) {
  const license = await fetchModelLicense(modelId);

  const userConfirmed = await showModal({
    title: "License Agreement",
    content: license.text,
    buttons: ["Cancel", "Accept and Download"]
  });

  if (!userConfirmed) return;

  startDownload(modelId);
}
```

#### 4.3 下载进度协议

Ollama 的 pull 进度可以通过以下方式获取：

1. **stdout 解析**（推荐）：
   ```
   pulling manifest
   pulling 6a0746a1ec1a... 32% ████████░░░░░░░░░░░░░░
   pulling 6a0746a1ec1a... 100% ████████████████████████
   verifying sha256 digest
   ```

2. **API 轮询**（备选）：
   ```bash
   GET http://127.0.0.1:11434/api/show?name=llama3
   ```

#### 4.4 UI 进度显示

```javascript
// 进度事件
{
  "model": "llama3",
  "status": "downloading",
  "progress": 0.42,  // 42%
  "downloaded": "2.1 GB",
  "total": "5.0 GB",
  "speed": "15 MB/s"
}
```

#### DoD

- [ ] 用户可以在 UI 中搜索和浏览模型
- [ ] 下载前强制展示许可证
- [ ] 进度条实时更新（100ms 刷新间隔）
- [ ] 支持暂停/恢复下载
- [ ] 磁盘空间不足时提前警告
- [ ] 下载失败时显示明确错误信息

---

### Phase 5: 首次启动体验（FTU）（2 周）

**目标**: 用户安装后 3 步内完成首次对话

#### 5.1 首次启动流程

```
安装完成
 ↓
启动 App
 ↓
[Step 1] 欢迎页面
  "Welcome to AgentOS"
  [Continue]
 ↓
[Step 2] 选择模型存储位置
  "Where would you like to store AI models?"
  • Default: ~/.agentos/models (20 GB available)
  • Custom: [Browse...]
  [Continue]
 ↓
[Step 3] 下载推荐模型
  "Download a starter model"
  • Llama 3.1 (8B) - Recommended (4.7 GB)
  • Skip (download later)
  [Download] [Skip]
 ↓
进入聊天界面
```

#### 5.2 磁盘空间预检

```javascript
function checkDiskSpace(path) {
  const available = getDiskSpace(path);

  if (available < 20 * 1024 * 1024 * 1024) {  // 20 GB
    showWarning({
      title: "Low Disk Space",
      message: "At least 20 GB free space is recommended for AI models.",
      buttons: ["Choose Different Location", "Continue Anyway"]
    });
  }
}
```

#### 5.3 错误恢复路径

| 错误场景 | 用户看到的提示 | 自动修复方案 |
|---------|--------------|------------|
| 端口 8000 被占用 | "AgentOS is using port 8001 instead" | 自动切换到 8001-8010 |
| Ollama 启动失败 | "Ollama server failed to start. Check logs?" | [Open Logs] [Restart] |
| 模型下载失败 | "Download interrupted. Retry?" | [Retry] [Cancel] |

#### DoD

- [ ] 首次启动流程在 3 分钟内完成（不含模型下载）
- [ ] 磁盘空间预检生效
- [ ] 所有错误都有明确的恢复路径
- [ ] 用户完成 FTU 后可以立即开始对话

---

### Phase 6: 跨平台打包与签名（3-4 周）

**目标**: 生成可分发的安装包，通过平台安全验证

#### 6.1 打包清单

| 平台 | 格式 | 签名 | 公证 |
|------|------|------|------|
| macOS (Intel) | `.dmg` | Apple Developer ID | Notarization |
| macOS (Apple Silicon) | `.dmg` | Apple Developer ID | Notarization |
| Windows | `.exe` (installer) | Authenticode | N/A |

#### 6.2 签名流程

**macOS**:
```bash
# 1. 签名所有二进制
codesign --deep --force --verify --verbose \
  --sign "Developer ID Application: YourName" \
  --timestamp \
  --options runtime \
  AgentOS.app

# 2. 打包 DMG
hdiutil create -volname "AgentOS" \
  -srcfolder AgentOS.app \
  -ov -format UDZO \
  AgentOS-v0.4.2-macos-arm64.dmg

# 3. 公证
xcrun notarytool submit AgentOS-v0.4.2-macos-arm64.dmg \
  --keychain-profile "AC_PASSWORD" \
  --wait

# 4. 钉选公证票据
xcrun stapler staple AgentOS-v0.4.2-macos-arm64.dmg
```

**Windows**:
```powershell
# 使用 SignTool
signtool sign /f certificate.pfx /p password /tr http://timestamp.digicert.com /td sha256 /fd sha256 AgentOS-Setup.exe
```

#### 6.3 测试矩阵

| 场景 | macOS Intel | macOS ARM | Windows 10 | Windows 11 |
|------|------------|-----------|-----------|------------|
| 安装 | ✓ | ✓ | ✓ | ✓ |
| 首次启动 | ✓ | ✓ | ✓ | ✓ |
| 下载模型 | ✓ | ✓ | ✓ | ✓ |
| 更新 App | ✓ | ✓ | ✓ | ✓ |
| 更新 Runtime | ✓ | ✓ | ✓ | ✓ |
| Gatekeeper/SmartScreen | ✓ | ✓ | ✓ | ✓ |

#### DoD

- [ ] 所有平台的安装包可以通过安全检查
- [ ] macOS：无 Gatekeeper 警告
- [ ] Windows：无 SmartScreen 警告
- [ ] 安装包大小 < 200 MB（不含模型）
- [ ] 卸载后清理干净（保留用户数据目录）

---

## 🚨 关键风险与缓解措施

### 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| **杀毒软件误报** | 高 | 高 | • 签名所有二进制<br>• 提交样本到主流杀软白名单<br>• 提供手动白名单添加指南 |
| **模型许可合规** | 中 | 高 | • 下载前强制许可证确认<br>• 记录用户同意时间戳<br>• 不预装模型 |
| **Ollama 版本兼容** | 中 | 中 | • 锁定 Ollama 版本范围<br>• 更新前测试兼容性<br>• 提供降级选项 |
| **更新失败导致无法启动** | 低 | 高 | • 强制备份机制<br>• 自动回滚<br>• Safe Mode 启动选项 |
| **跨平台端口冲突** | 中 | 低 | • 自动端口扫描<br>• 备用端口范围<br>• 用户可手动指定 |

---

## 📊 项目里程碑

### 总体时间线（16-20 周）

```
Phase 0: 基础设施         [██░░░░░░░░] Week 1-2
Phase 1: Sidecar 管理     [░░██████░░░░] Week 2-5
Phase 2: Runtime 打包     [░░░░░██████░] Week 5-8
Phase 3: 热更新          [░░░░░░░████░░] Week 8-12
Phase 4: 模型管理        [░░░░░░░░░███░] Week 12-15
Phase 5: FTU            [░░░░░░░░░░██░] Week 15-17
Phase 6: 打包签名        [░░░░░░░░░░░██] Week 17-20
```

### 关键决策点（Go/No-Go）

| 里程碑 | 决策标准 | 决策人 |
|--------|---------|-------|
| Phase 0 完成 | 所有架构文档签字确认 | 技术负责人 |
| Phase 3 完成 | 更新机制在 3 个平台上通过测试 | QA Lead |
| Beta 发布 | 10 个用户完成 FTU，0 个 P0 bug | Product Owner |

---

## 📦 可交付物清单

### 技术文档

- [ ] `DIRECTORY_STRUCTURE.md` - 目录结构与约束
- [ ] `VERSION_PROTOCOL.md` - 版本管理协议
- [ ] `UPDATE_BOUNDARIES.md` - 更新边界声明
- [ ] `SIDECAR_LIFECYCLE.md` - Sidecar 生命周期管理
- [ ] `PACKAGING_GUIDE.md` - 打包与签名指南
- [ ] `ROLLBACK_STRATEGY.md` - 回滚策略
- [ ] `FTU_FLOW.md` - 首次启动流程

### 用户文档

- [ ] `INSTALLATION_GUIDE.md` - 安装指南
- [ ] `TROUBLESHOOTING.md` - 故障排除
- [ ] `MODEL_LICENSE_FAQ.md` - 模型许可证 FAQ
- [ ] `UPDATE_FAQ.md` - 更新常见问题

### 代码仓库

- [ ] `agentos-desktop/` - Tauri 桌面应用
- [ ] `agentos-desktop/src-tauri/` - Rust 后端
- [ ] `agentos-desktop/src/` - React 前端
- [ ] `agentos-desktop/updater-helper/` - 更新辅助程序
- [ ] `agentos-desktop/scripts/package.sh` - 打包脚本
- [ ] `agentos-desktop/scripts/sign-macos.sh` - macOS 签名脚本
- [ ] `agentos-desktop/scripts/sign-windows.ps1` - Windows 签名脚本

### CI/CD Pipeline

- [ ] GitHub Actions: 自动打包（macOS Intel/ARM, Windows）
- [ ] GitHub Actions: 自动签名
- [ ] GitHub Actions: 更新 manifest.json
- [ ] GitHub Actions: 发布到 releases.agentos.com

---

## 🎯 成功标准（验收标准）

### 核心体验标准

1. **安装体验**
   - [ ] macOS：拖拽到 Applications 即可使用
   - [ ] Windows：双击 `.exe` 完成安装
   - [ ] 安装后首次启动时间 < 10 秒

2. **首次启动（FTU）**
   - [ ] 3 步内完成配置
   - [ ] 磁盘空间不足时有明确警告
   - [ ] 完成 FTU 后可以立即对话

3. **核心功能**
   - [ ] 聊天对话响应时间 < 5 秒（首次响应）
   - [ ] "Open WebUI" 按钮正常工作
   - [ ] 模型下载进度实时更新

4. **更新体验**
   - [ ] 更新检测到提示用户 < 5 秒
   - [ ] 下载进度明确显示
   - [ ] 更新后重启，用户无感知

5. **稳定性**
   - [ ] Sidecar 异常退出时自动重启
   - [ ] App 崩溃后重启，状态恢复
   - [ ] 更新失败时自动回滚

### 性能标准

| 指标 | 目标值 |
|------|-------|
| App 启动时间 | < 3 秒 |
| Sidecar 启动时间 | < 5 秒 |
| 首次响应时间（聊天） | < 5 秒 |
| 内存占用（空闲） | < 500 MB |
| 安装包大小 | < 200 MB |

### 安全标准

- [ ] 所有二进制已签名
- [ ] macOS 通过 Gatekeeper 和 Notarization
- [ ] Windows 通过 SmartScreen
- [ ] 无杀毒软件误报（已提交白名单）
- [ ] 网络流量仅限 127.0.0.1

---

## 🔄 迭代策略

### Alpha（内部测试）

- **目标用户**: 团队成员
- **覆盖**: macOS ARM（开发机器）
- **重点**: 核心功能验证

### Beta（早期采用者）

- **目标用户**: 10-20 个外部测试者
- **覆盖**: macOS Intel/ARM + Windows
- **重点**: FTU 体验 + 稳定性

### v1.0（公开发布）

- **目标用户**: 所有用户
- **覆盖**: 全平台
- **重点**: 完整功能 + 文档

---

## 📞 联系与支持

### 项目团队

- **技术负责人**: TBD
- **前端开发**: TBD
- **Rust 开发**: TBD
- **QA Lead**: TBD
- **产品经理**: TBD

### 决策流程

- **架构变更**: 需技术负责人批准
- **用户体验变更**: 需产品经理批准
- **发布节奏**: 每两周一次 Sprint Review

---

## 📚 参考资料

### 技术文档

- [Tauri 官方文档](https://tauri.app/v2/guide/)
- [Tauri Sidecar 指南](https://tauri.app/v2/guides/building/sidecar/)
- [Ollama API 文档](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [Nuitka 打包指南](https://nuitka.net/doc/user-manual.html)

### 合规参考

- [Ollama License (MIT)](https://github.com/ollama/ollama/blob/main/LICENSE)
- [Llama 3 License](https://llama.meta.com/llama3/license/)
- [Apple Developer ID 签名](https://developer.apple.com/support/code-signing/)

---

## 📝 变更日志

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| 1.0 | 2026-01-30 | 初始版本 | Claude |

---

**下一步行动**: 召开架构评审会议，冻结 Phase 0 设计
