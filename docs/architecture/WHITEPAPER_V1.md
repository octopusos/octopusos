# AgentOS v1.0 白皮书

**From Natural Language to Auditable Execution**

---

## 一句话定义

**AgentOS 是一个让 AI"把活干完"，但不失控、不越权、可审计、可回滚的执行操作系统。**

它不是一个模型，不是一个 Copilot，也不是一个自动执行脚本。  
它是 AI 执行的"操作系统级"治理层。

---

## 为什么需要 AgentOS？

过去一年，我们看到了大量 AI 工具：
- 能写代码
- 能生成方案
- 能提出建议

但真正的问题一直没解决：

**👉 谁来保证 AI 的"执行"是安全、可控、可追责的？**

现实世界里，执行意味着：
- 改代码
- 写文件
- 跑命令
- 影响生产系统
- 需要审查、回滚和审计

**"能写" ≠ "能执行"**  
而这中间，缺的不是模型能力，是**工程级约束体系**。

---

## AgentOS 解决的不是"更聪明的 AI"，而是"更可靠的执行"

AgentOS 把 AI 执行拆成**清晰、可验证的阶段**：

```
自然语言
 → 意图（Intent）
 → 决策（Coordinator）
 → 计划（Dry-Executor）
 → 回答（AnswerPack）
 → 执行（Executor / Tool）
 → 审计（Audit）
```

每一步都有结构、有边界、有门禁。

---

## AgentOS 的核心设计原则

### 1️⃣ 规划与执行彻底分离

AgentOS 明确区分：
- **Dry Run（规划）**：只生成 "打算做什么"
- **Execution（执行）**：只有在通过审查、门禁后才允许发生

**AI 永远不能"一边想一边做"。**

---

### 2️⃣ BLOCKED 是一等状态，而不是错误

当信息不足时，AgentOS 不会瞎编、不会硬跑：
- 自动生成 **QuestionPack**
- 系统进入 **BLOCKED** 状态
- 必须由人类通过 **AnswerPack** 解锁

**这是对现实世界的尊重，而不是能力不足。**

---

### 3️⃣ 执行必须受控、可回滚、可审计

所有真实执行都满足：
- **Allowlist**（白名单动作）
- **Sandbox**（隔离环境）
- **Lock**（防并发踩踏）
- **Review Gate**（高风险审批）
- **Audit Log**（完整执行记录）
- **Rollback**（失败可恢复）

**AgentOS 不追求"快"，而是追求"不会出事"。**

---

### 4️⃣ 工具是"外包工人"，不是系统主脑

AgentOS 可以把执行外包给工具：
- OpenCode
- Codex
- Claude CLI
- 未来任何 Agent / CLI

但工具只做三件事：
1. 接收任务包（Task Pack）
2. 执行
3. 返回结果包（Result Pack）

**最终裁决权始终在 AgentOS。**

---

## 一个真实的执行闭环（示意）

```
用户：
"给这个项目加一个 API，并补测试。"

↓

AgentOS：
- 生成 Intent
- 规划 3 个 commit
- 标记为中风险

↓

AgentOS：
- 生成 Dry Execution Plan
- 需要确认测试范围 → BLOCKED

↓

人类：
- AnswerPack 回填

↓

AgentOS：
- 生成执行请求
- 外包给 Claude CLI

↓

Claude CLI：
- 返回 diff / test logs

↓

AgentOS：
- Gate 校验
- Snapshot 对比
- 审计记录

↓

结果：
- 可合并 PR
- 全程可追溯
```

---

## AgentOS v1.0 已具备的能力

- ✅ 自然语言 → 可执行计划
- ✅ 自动识别风险、阻塞不确定执行
- ✅ 人机协作解锁（AnswerPack）
- ✅ 本地受控执行（Sandbox + Allowlist）
- ✅ 外包执行（OpenCode / Codex / Claude CLI）
- ✅ 全链路审计、回滚、冻结、验收
- ✅ 所有关键行为 **机器门禁而非人工约定**

---

## AgentOS 不是什么？

为了避免误解，明确三点：
- ❌ 不是"自动写代码然后直接跑"
- ❌ 不是"让 AI 拥有系统权限"
- ❌ 不是"更聪明的 Copilot"

**AgentOS 是"执行治理系统"，不是模型。**

---

## 技术实现概览

### 三种执行模式

| 模式 | 提问能力 | 适用场景 |
|------|---------|---------|
| **interactive** | 自由提问 | 探索性任务、需要人类决策 |
| **semi_auto** | 仅允许 blocker（有预算） | 大部分自动化任务 |
| **full_auto** | question_budget = 0 | 完全确定的任务 |

### 外置记忆服务（MemoryPack）

AgentOS 将 AI 的"记忆"与"执行"分离：
- **MemoryItem**: 项目规范、约定、约束
- **MemoryPack**: 上下文自动构建
- **FTS 搜索**: 全文索引，相关记忆自动注入

```bash
# 添加项目规范
uv run agentos memory add \
  --type convention \
  --summary "React 组件使用 PascalCase 命名" \
  --tags frontend,react
```

### 智能锁机制

- **任务级锁**: 防止同一任务被多个 worker 执行
- **文件级锁**: 防止并发修改同一文件
- **WAITING_LOCK 状态**: 等待锁释放后自动 rebase

### 全链路审计（ReviewPack）

每次执行自动生成：
- 执行计划摘要
- 变更文件列表
- Patch 意图 + diff hash
- Commit hash 绑定
- Rollback 指南

```json
{
  "task_id": "task-001",
  "patches": [
    {
      "patch_id": "p001",
      "intent": "为什么改",
      "files": ["src/auth.ts"],
      "diff_hash": "sha256..."
    }
  ],
  "commits": [
    {"hash": "abc123", "message": "feat: ..."}
  ],
  "rollback_guide": "git revert abc123^..HEAD"
}
```

---

## 10 条护城河（v1.0 红线）

AgentOS v1.0 的执行质量通过 10 条机器门禁保证：

1. ✅ 无 MemoryPack 不允许执行（哪怕为空）
2. ✅ full_auto question_budget = 0（提问即违规）
3. ✅ 任何命令/路径禁止编造（来源可追溯）
4. ✅ 每次执行必须写 run_steps（Plan/Apply/Verify 不可缺）
5. ✅ 每次执行必须有 review_pack.md
6. ✅ 每个 patch 必须记录 intent + 文件列表 + diff hash
7. ✅ 每次发布必须绑定 commit hash
8. ✅ 文件锁冲突必须 WAIT 并 rebase
9. ✅ 并发执行必须受 locks 限制
10. ✅ scheduler 触发必须可复现（cron 规则可审计）

**这些不是"建议"，而是机器强制执行的约束。**

---

## 离真正的"全自动执行"还差什么？

AgentOS v1.0 刻意停在安全边界内。  
未来版本可能逐步引入：
- 更丰富的执行沙箱
- 更细粒度的审批流
- CI / PR / ChatOps 深度集成

**但原则不变：**

**任何自动化，都必须先能被约束、审计和否决。**

---

## 总结

**AI 会越来越强，但执行不能靠信任。**

AgentOS 的目标不是"让 AI 更大胆"，  
而是让 AI 的执行**第一次变得像真正的软件系统**：
- 有状态
- 有边界
- 有审计
- 有责任

**这不是一个工具，而是一种新的执行范式。**

---

## 快速开始

### 安装

```bash
git clone https://github.com/yourusername/agentos.git
cd agentos
pip install uv
uv sync
```

### 基础使用

```bash
# 1. 初始化
uv run agentos init

# 2. 注册项目
uv run agentos project add /path/to/project --id my-project

# 3. 添加记忆
uv run agentos memory add \
  --type convention \
  --summary "React 组件使用 PascalCase 命名"

# 4. 创建任务（半自动模式）
cat > queue/task.task.json <<EOF
{
  "task_id": "task-001",
  "project_id": "my-project",
  "execution_mode": "semi_auto",
  "execution_policy": {
    "question_budget": 3,
    "risk_profile": "safe"
  }
}
EOF

# 5. 执行
uv run agentos orchestrate
```

---

## 社交媒体版本

### 微信朋友圈（建议配图）

```
这一年做 AI，最大的感受是：
"能写代码"不是难点，
"敢不敢执行"才是。

我把这一套执行治理体系叫做 AgentOS v1.0。

它不是让 AI 更聪明，
而是让 AI 第一次能被信任。

从自然语言到可审计执行，
这是 AI 工程化的下一步。

[链接] github.com/yourusername/agentos
```

### LinkedIn (English)

```
After a year working with AI agents, I realized the real problem 
isn't intelligence — it's execution governance.

The gap between "AI can write code" and "AI can safely execute" 
is not about capability, it's about engineering constraints.

This is why I built AgentOS.

AgentOS is an OS-level governance layer that makes AI execution:
• Structured (Intent → Plan → Execution → Audit)
• Controlled (Allowlist, Sandbox, Gates)
• Auditable (Full trace, rollback, review packs)
• Collaborative (BLOCKED state, AnswerPack)

It's not about making AI bolder — 
it's about making AI execution trustworthy.

From Natural Language to Auditable Execution.

[Link] github.com/yourusername/agentos

#AI #MLOps #SoftwareEngineering #AIGovernance
```

### 推特/X (Thread 版本)

```
1/ AI 能写代码了，但谁敢让它直接执行？

这个问题困扰了我一整年。

2/ 问题不在模型能力，而在工程约束：
• 如何防止 AI 编造命令？
• 如何审计执行过程？
• 如何在出错时回滚？
• 如何平衡自动化与安全？

3/ 我把解决方案叫做 AgentOS —— 
AI 执行的操作系统级治理层。

核心思想：
把"规划"和"执行"彻底分离，
让每一步都可验证、可回滚。

4/ AgentOS 把 AI 执行拆成 6 个阶段：

自然语言 
→ Intent（意图）
→ Coordinator（决策）
→ Dry Run（规划）
→ AnswerPack（解锁）
→ Execution（执行）
→ Audit（审计）

每一步都有结构、有边界、有门禁。

5/ 三个关键设计：

1️⃣ BLOCKED 是一等状态
信息不足时，AI 不瞎编，而是生成 QuestionPack 等待人类解答。

2️⃣ 执行必须受控
Allowlist + Sandbox + Lock + Gate + Audit Log

3️⃣ 工具是外包工人
可以用 OpenCode/Codex/Claude CLI，
但最终决策权在 AgentOS。

6/ v1.0 已具备：
✅ 三种执行模式（interactive/semi_auto/full_auto）
✅ 外置记忆服务（MemoryPack）
✅ 智能锁机制（任务级 + 文件级）
✅ 全链路审计（ReviewPack）
✅ 10 条机器门禁（不是建议，是约束）

7/ AgentOS 不追求"更聪明的 AI"，
而是让 AI 执行第一次变得像软件系统：
• 有状态
• 有边界
• 有审计
• 有责任

这不是工具，是新的执行范式。

8/ 开源地址：
github.com/yourusername/agentos

欢迎讨论、贡献、挑战。

From Natural Language to Auditable Execution.
```

---

## 下一步建议

你可以选择：

1. **✍️ 英文 LinkedIn 版白皮书**（更详细的英文版本）
2. **🧠 三张"思想级架构图"**（可视化核心理念）
3. **🧪 技术反思文**《为什么多数 AI Agent 注定失控》
4. **📊 对比分析**《AgentOS vs. LangGraph/AutoGPT/Devin》
5. **🎬 Demo 视频脚本**（15 分钟演示）

你想下一步做哪个？

---

**AgentOS v1.0**  
*Natural Language → Auditable Execution*

**License**: MIT  
**Maintainer**: AgentOS Team  
**Status**: 🟢 Active Development  
**Last Updated**: 2026-01-25
