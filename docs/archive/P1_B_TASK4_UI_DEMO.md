# P1-B Task 4: Explain Drawer 搜索建议 - UI 演示

## 界面布局示意图

```
┌─────────────────────────────────────────────────────────┐
│  Explain Drawer                                     [×] │
├─────────────────────────────────────────────────────────┤
│  🧠 Explain: task-orchestration                         │
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐ │
│  │ 🔍 Search other entities...                       │ │ ← 新增搜索框
│  └───────────────────────────────────────────────────┘ │
│    ┌───────────────────────────────────────────────┐   │
│    │ ✅ file   task/manager.py                     │   │ ← Safe 实体
│    │    Safe: 5 evidence, covered by git+doc+code  │   │
│    ├───────────────────────────────────────────────┤   │
│    │ ✅ task   task-execution                      │   │
│    │    Safe: 3 evidence, covered by git+doc       │   │
│    ├───────────────────────────────────────────────┤   │
│    │ ⚠️  capability   task-runner                  │   │ ← Warning 实体
│    │    Moderate risk: partial code coverage       │   │
│    ├───────────────────────────────────────────────┤   │
│    │ 🚨 governance   governance-engine             │   │ ← Dangerous 实体
│    │    High-risk: No documentation coverage       │   │
│    │    ⚠️ High-risk blind spot: No doc coverage,  │   │ ← 高危警告
│    │       high fan-in (15 dependents)             │   │
│    └───────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  [Why] [Impact] [Trace] [Map]                           │ ← 现有 Tabs
├─────────────────────────────────────────────────────────┤
│                                                          │
│  📊 Evidence Sources: GIT DOC CODE (3/3 sources)        │
│  ✅ This explanation is based on all sources...         │
│                                                          │
│  Found 3 paths explaining this entity, supported by...  │
│                                                          │
│  Path #1                                                 │
│    [doc] ADR-001-Task-System                            │
│    ↓ documents                                          │
│    [file] task/manager.py                               │
│    ↓ implements                                         │
│    [term] task-orchestration                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 搜索建议卡片设计

### Safe 实体（绿色主题）

```
┌─────────────────────────────────────────────────────┐
│ ✅ file   task/manager.py                           │
│ ─────────────────────────────────────────────────── │
│ Safe: 5 evidence, covered by git+doc+code           │
└─────────────────────────────────────────────────────┘
```

**配色**:
- 背景色：`#f0f9f4`（淡绿）
- 图标：`#28a745`（绿色 check_circle）
- 提示文本：`#28a745`（绿色）

---

### Warning 实体（黄色主题）

```
┌─────────────────────────────────────────────────────┐
│ ⚠️  capability   task-runner                        │
│ ─────────────────────────────────────────────────── │
│ Moderate risk: partial code coverage, no git refs   │
└─────────────────────────────────────────────────────┘
```

**配色**:
- 背景色：`#fffbf0`（淡黄）
- 图标：`#ffc107`（黄色 warning）
- 提示文本：`#d39e00`（深黄）

---

### Dangerous 实体（红色主题）

```
┌─────────────────────────────────────────────────────┐
│ 🚨 governance   governance-engine                   │
│ ─────────────────────────────────────────────────── │
│ High-risk: No documentation coverage                │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ⚠️ High-risk blind spot:                        │ │
│ │ No documentation coverage, high fan-in (15      │ │
│ │ dependents). Suggest: Add ADR or design doc.    │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**配色**:
- 背景色：`#fff5f5`（淡红）
- 图标：`#dc3545`（红色 emergency）
- 提示文本：`#dc3545`（红色）
- 警告框背景：`#fff5f5`（淡红）
- 警告框边框：`3px solid #dc3545`（红色左边框）

---

## 交互状态演示

### 1. 空状态（无输入）

```
┌───────────────────────────────────────────────────┐
│ 🔍 Search other entities...                       │
└───────────────────────────────────────────────────┘
```

**说明**: 搜索框空白，无下拉框

---

### 2. 输入中（< 2 字符）

```
┌───────────────────────────────────────────────────┐
│ 🔍 t|                                              │
└───────────────────────────────────────────────────┘
```

**说明**: 输入 1 个字符，不触发搜索

---

### 3. 加载中（≥ 2 字符，300ms 内）

```
┌───────────────────────────────────────────────────┐
│ 🔍 task|                                           │
└───────────────────────────────────────────────────┘
```

**说明**: 输入 "task"，等待 debounce 300ms

---

### 4. 显示建议（API 返回）

```
┌───────────────────────────────────────────────────┐
│ 🔍 task|                                           │
└───────────────────────────────────────────────────┘
  ┌───────────────────────────────────────────────┐
  │ ✅ file   task/manager.py                     │
  ├───────────────────────────────────────────────┤
  │ ✅ task   task-execution                      │
  ├───────────────────────────────────────────────┤
  │ ⚠️  capability   task-runner                  │
  └───────────────────────────────────────────────┘
```

**说明**: 下拉框显示，最多 10 个建议

---

### 5. 键盘选中（Arrow Down）

```
┌───────────────────────────────────────────────────┐
│ 🔍 task|                                           │
└───────────────────────────────────────────────────┘
  ┌───────────────────────────────────────────────┐
  │ ✅ file   task/manager.py                     │ ← 灰色背景
  ├═══════════════════════════════════════════════┤
  │ ✅ task   task-execution                      │ ← 深灰背景（选中）
  ├───────────────────────────────────────────────┤
  │ ⚠️  capability   task-runner                  │
  └───────────────────────────────────────────────┘
```

**说明**: 按 ↓ 键，第二项高亮

---

### 6. 无结果（无匹配实体）

```
┌───────────────────────────────────────────────────┐
│ 🔍 xyz|                                            │
└───────────────────────────────────────────────────┘
```

**说明**: 输入 "xyz"，无匹配实体，不显示下拉框

---

## 键盘导航示意图

```
┌─────────────────────────────────────────────────┐
│  Arrow Down (↓)    → 向下选择                   │
│  Arrow Up (↑)      → 向上选择                   │
│  Enter (⏎)         → 确认选择，切换实体        │
│  Escape (Esc)      → 关闭下拉框                 │
└─────────────────────────────────────────────────┘
```

### 导航演示

```
初始状态（无选中）:
  ┌───────────────────────────────────────────────┐
  │ ✅ item-1                                     │
  │ ✅ item-2                                     │
  │ ⚠️  item-3                                    │
  └───────────────────────────────────────────────┘

按 ↓ 键一次:
  ┌═══════════════════════════════════════════════┐
  │ ✅ item-1                                     │ ← 选中
  ├───────────────────────────────────────────────┤
  │ ✅ item-2                                     │
  │ ⚠️  item-3                                    │
  └───────────────────────────────────────────────┘

按 ↓ 键两次:
  ┌───────────────────────────────────────────────┐
  │ ✅ item-1                                     │
  ├═══════════════════════════════════════════════┤
  │ ✅ item-2                                     │ ← 选中
  ├───────────────────────────────────────────────┤
  │ ⚠️  item-3                                    │
  └───────────────────────────────────────────────┘

按 Enter:
  → 切换到 item-2 实体
  → Header 更新为 "🧠 Explain: item-2"
  → 自动查询当前 Tab（Why/Impact/Trace/Map）
```

---

## 响应式设计（移动端）

### 桌面端（宽度 > 768px）

```
┌─────────────────────────────────────────────────────────┐
│  Explain: task-orchestration                        [×] │ ← 500px 宽
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐ │
│  │ 🔍 Search other entities...                       │ │
│  └───────────────────────────────────────────────────┘ │
│                                                          │
│  [Why] [Impact] [Trace] [Map]                           │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

### 移动端（宽度 ≤ 768px）

```
┌─────────────────────────────────┐
│  Explain: task-orchestration [×]│ ← 100% 宽
├─────────────────────────────────┤
│  ┌───────────────────────────┐ │
│  │ 🔍 Search entities...     │ │ ← 更短的 placeholder
│  └───────────────────────────┘ │
│                                 │
│  [Why] [Impact]                 │ ← 两行显示 Tabs
│  [Trace] [Map]                  │
│                                 │
└─────────────────────────────────┘
```

**适配要点**:
- Drawer 宽度从 500px 变为 100%
- 搜索框宽度自适应
- Tab 按钮缩小字体（14px → 13px）
- 下拉框左右边距从 20px 变为 10px

---

## 颜色系统

### 主题色

| 元素 | 颜色代码 | 用途 |
|-----|---------|------|
| **主蓝色** | `#007bff` | 输入框聚焦边框、Tab 激活 |
| **安全绿** | `#28a745` | Safe 实体图标和文本 |
| **警告黄** | `#ffc107` | Warning 实体图标 |
| **警告深黄** | `#d39e00` | Warning 实体文本 |
| **危险红** | `#dc3545` | Dangerous 实体图标和文本 |
| **背景灰** | `#fafafa` | 搜索容器背景 |
| **边框灰** | `#ddd` | 输入框和下拉框边框 |

### 渐变背景（Hover 状态）

| 安全等级 | 默认背景 | Hover 背景 |
|---------|---------|-----------|
| Safe | `white` | `#f0f9f4` |
| Warning | `white` | `#fffbf0` |
| Dangerous | `white` | `#fff5f5` |

---

## 字体系统

| 元素 | 字体大小 | 字重 | 颜色 |
|-----|---------|------|------|
| **搜索输入框** | 14px | 400 | `#333` |
| **实体类型标签** | 11px | 600 | `white` |
| **实体名称** | 14px | 500 | `#333` |
| **提示文本** | 12px | 400 | 动态（按安全等级）|
| **警告文本** | 12px | 400 | `#dc3545` |
| **警告标题** | 12px | 600 | `#dc3545` |

---

## 动画效果

### 下拉框展开动画

```css
.entity-search-dropdown {
    animation: slideDown 0.2s ease-out;
}

@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

### Hover 过渡

```css
.entity-search-item {
    transition: background 0.2s ease;
}
```

### 输入框聚焦

```css
#entity-search-input {
    transition: border-color 0.2s ease;
}

#entity-search-input:focus {
    border-color: #007bff;
}
```

---

## Material Icons 使用

| 图标名 | 用途 | 安全等级 |
|-------|------|---------|
| `check_circle` | Safe 实体 | Safe |
| `warning` | Warning 实体 | Warning |
| `emergency` | Dangerous 实体 | Dangerous |
| `bar_chart` | Coverage Badge | - |
| `psychology` | Drawer 标题 | - |

**引入方式**:
```html
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
```

**使用示例**:
```html
<span class="material-icons">check_circle</span>
```

---

## 实际效果截图说明

由于这是代码实现，无法直接截图，但以下是测试步骤：

### 测试步骤

1. **启动 WebUI**:
   ```bash
   agentos webui
   ```

2. **打开任意 Explain Drawer**:
   - 进入 Tasks 视图
   - 点击任务卡片上的 🧠 Explain 按钮

3. **测试搜索功能**:
   - 在搜索框输入 "task"
   - 观察下拉建议列表
   - 点击任一建议切换实体

4. **测试键盘导航**:
   - 输入搜索关键词
   - 按 ↓ 键选择建议
   - 按 Enter 确认选择

5. **测试高危警告**:
   - 搜索高危实体（如 "governance"）
   - 观察红色警告框和原因说明

---

## 用户反馈收集点

### 关键指标

1. **搜索使用率**: 用户是否主动使用搜索框？
2. **安全建议采纳率**: 用户是否优先选择 Safe 实体？
3. **高危警告阅读率**: 用户是否查看 Dangerous 实体的警告？
4. **键盘导航使用率**: 用户是否使用键盘快捷键？

### 优化方向

- 如果搜索使用率低 → 考虑添加"热门实体"默认建议
- 如果高危警告忽略率高 → 增强视觉警告强度
- 如果键盘导航使用率低 → 添加快捷键提示

---

**UI 演示文档结束**
