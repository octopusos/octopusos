id: cmd_component_spec_create
type: command
version: 0.8.0
category: design
title: "Create Component Spec"
description: "Create a component spec including states, interactions, and accessibility notes."

recommended_roles:
  - ui_ux_designer

workflow_links:
  - workflow: detailed_design
    phases: [component_spec]

inputs:
  - name: component_name
    type: string
    required: true
  - name: states
    type: string[]
    required: false

outputs:
  - name: component_spec_ref
    type: string

preconditions:
  - "requirements_clarified"

effects:
  - scope: docs
    kind: write
    description: "Creates a component specification artifact."

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
