id: cmd_design_token_update
type: command
version: 0.8.0
category: design
title: "Update Design Tokens"
description: "Update design tokens and document rationale and impact."

recommended_roles:
  - ui_ux_designer

workflow_links:
  - workflow: detailed_design
    phases: [design_system]

inputs:
  - name: token_changes
    type: string
    required: true

outputs:
  - name: token_change_ref
    type: string

preconditions:
  - "design_system_defined"

effects:
  - scope: design_system
    kind: write
    description: "Updates design token definitions."

risk_level: medium
evidence_required: true

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
