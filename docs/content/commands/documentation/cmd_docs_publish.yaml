id: cmd_docs_publish
type: command
version: 0.8.0
category: documentation
title: "Publish Documentation"
description: "Define documentation publishing steps and required checks."

recommended_roles:
  - technical_writer
  - devops_engineer

workflow_links:
  - workflow: release_management
    phases: [communication]

inputs:
  - name: target
    type: string
    required: true
    description: "Publishing target, e.g. internal|public."

outputs:
  - name: publish_record_ref
    type: string

preconditions:
  - "docs_ready"

effects:
  - scope: docs
    kind: write

risk_level: medium
evidence_required: true

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
