id: cmd_generate_api_client
type: command
version: 0.8.0
category: engineering
title: "Generate API Client"
description: "Generate or update API client artifacts based on API specification."

recommended_roles:
  - frontend_engineer
  - backend_engineer

workflow_links:
  - workflow: feature_implementation
    phases: [integration]

inputs:
  - name: api_spec_ref
    type: string
    required: true

outputs:
  - name: client_artifact_ref
    type: string

preconditions:
  - "api_spec_available"

effects:
  - scope: repo
    kind: write
    description: "Updates generated client artifacts."

risk_level: medium
evidence_required: true

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
