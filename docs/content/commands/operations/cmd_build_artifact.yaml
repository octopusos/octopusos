id: cmd_build_artifact
type: command
version: 0.8.0
category: operations
title: "Build Artifact"
description: "Define a build artifact creation procedure and expected outputs."

recommended_roles:
  - devops_engineer

workflow_links:
  - workflow: deployment_planning
    phases: [build]

inputs:
  - name: target
    type: string
    required: true

outputs:
  - name: artifact_ref
    type: string

preconditions:
  - "build_pipeline_defined"

effects:
  - scope: docs
    kind: write

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
