id: cmd_deploy_staging
type: command
version: 0.8.0
category: operations
title: "Deploy to Staging"
description: "Define deployment procedure to staging and verification steps."

recommended_roles:
  - devops_engineer
  - sre_engineer

workflow_links:
  - workflow: deployment_planning
    phases: [deploy]

inputs:
  - name: release_version
    type: string
    required: true

outputs:
  - name: staging_deploy_ref
    type: string

preconditions:
  - "artifact_available"
  - "staging_environment_ready"

effects:
  - scope: environment
    kind: write

risk_level: medium
evidence_required: true

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
