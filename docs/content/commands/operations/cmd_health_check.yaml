id: cmd_health_check
type: command
version: 0.8.0
category: operations
title: "Run Health Check"
description: "Define health check procedure and expected signals."

recommended_roles:
  - sre_engineer
  - devops_engineer

workflow_links:
  - workflow: deployment_planning
    phases: [verification]
  - workflow: incident_response
    phases: [triage]

inputs:
  - name: target_service
    type: string
    required: true

outputs:
  - name: health_check_result_ref
    type: string

preconditions:
  - "observability_available"

effects:
  - scope: docs
    kind: write

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
