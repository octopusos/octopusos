id: cmd_observability_dashboard_check
type: command
version: 0.8.0
category: operations
title: "Observability Dashboard Check"
description: "Define checklist for dashboards/alerts validation during release or incident."

recommended_roles:
  - sre_engineer

workflow_links:
  - workflow: performance_analysis
    phases: [signals]
  - workflow: incident_response
    phases: [triage]

inputs:
  - name: dashboard_scope
    type: string
    required: true

outputs:
  - name: dashboard_check_notes_ref
    type: string

preconditions:
  - "dashboards_defined"

effects:
  - scope: docs
    kind: write

risk_level: medium
evidence_required: true

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
