id: cmd_prd_create
type: command
version: 0.8.0
category: product
title: "Create PRD"
description: "Create a Product Requirements Document with scope, goals, and constraints."

recommended_roles:
  - product_manager

workflow_links:
  - workflow: requirements_definition
    phases: [documentation]

inputs:
  - name: problem_statement
    type: string
    required: true
  - name: target_users
    type: string[]
    required: false

outputs:
  - name: prd_id
    type: string

preconditions:
  - "problem_defined"

effects:
  - scope: docs
    kind: write
    description: "Creates a PRD artifact."

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
