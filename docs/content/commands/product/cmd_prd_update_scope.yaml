id: cmd_prd_update_scope
type: command
version: 0.8.0
category: product
title: "Update PRD Scope"
description: "Update PRD scope and record tradeoffs and rationale."

recommended_roles:
  - product_manager
  - project_manager

workflow_links:
  - workflow: requirements_definition
    phases: [revision]

inputs:
  - name: prd_id
    type: string
    required: true
  - name: scope_changes
    type: string
    required: true

outputs:
  - name: updated_prd_id
    type: string

preconditions:
  - "prd_exists"

effects:
  - scope: docs
    kind: write
    description: "Updates PRD content."

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
