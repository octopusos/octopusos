id: cmd_test_plan_create
type: command
version: 0.8.0
category: quality
title: "Create Test Plan"
description: "Create a test plan covering scope, environments, and acceptance criteria."

recommended_roles:
  - qa_engineer
  - product_manager

workflow_links:
  - workflow: testing_strategy
    phases: [planning]

inputs:
  - name: feature_id
    type: string
    required: true
  - name: acceptance_criteria_ref
    type: string
    required: false

outputs:
  - name: test_plan_ref
    type: string

preconditions:
  - "requirements_available"

effects:
  - scope: docs
    kind: write

risk_level: low
evidence_required: false

constraints:
  executable_payload: forbidden
  agent_binding: forbidden

lineage:
  introduced_in: v0.8
  derived_from: null
  supersedes: []
