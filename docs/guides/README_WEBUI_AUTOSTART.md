# ✨ WebUI 自动启动功能

> **AgentOS v0.3.0 新特性**: WebUI 现在可以自动启动！

---

## 🎯 什么是 WebUI 自动启动？

从 v0.3.0 开始，AgentOS 支持在运行**任何 CLI 命令**时自动在后台启动 WebUI。

### 核心特性

- ✅ **零配置**: 默认启用，无需手动启动
- ✅ **智能检测**: 自动检测是否已运行，避免重复启动
- ✅ **后台运行**: 不阻塞主命令执行
- ✅ **独立进程**: WebUI 进程独立，不受主命令影响
- ✅ **可配置**: 完全可控，随时启用/禁用

---

## 🚀 快速体验

### 只需运行任何命令

```bash
# 运行任何 AgentOS 命令
agentos task list

# WebUI 自动启动完成！
# 访问: http://127.0.0.1:8080
```

### 验证状态

```bash
agentos webui status
```

输出:
```
┏━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Property ┃ Value                      ┃
┡━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ Running  │ ✅ Yes                     │
│ PID      │ 12345                      │
│ URL      │ http://127.0.0.1:8080      │
└──────────┴────────────────────────────┘
```

---

## 🎛️ 管理命令

### 查看状态

```bash
agentos webui status
```

### 停止 WebUI

```bash
agentos webui stop
```

### 重启 WebUI

```bash
agentos webui restart
```

### 配置管理

```bash
# 查看配置
agentos webui config --show

# 禁用自动启动
agentos webui config --no-auto-start

# 启用自动启动
agentos webui config --auto-start

# 修改端口
agentos webui config --port 8888
```

---

## ⚙️ 配置选项

所有配置存储在 `~/.agentos/settings.json`:

```json
{
  "webui_auto_start": true,     // 是否自动启动
  "webui_host": "127.0.0.1",    // 绑定主机
  "webui_port": 8080            // 绑定端口
}
```

---

## 📂 相关文件

| 文件 | 说明 |
|------|------|
| `~/.agentos/settings.json` | 配置文件 |
| `~/.agentos/webui.pid` | 进程 ID |
| `~/.agentos/webui.log` | 运行日志 |

---

## 🎓 使用场景

### 场景 1: 日常使用 (默认)

```bash
# 自动启动已启用
agentos task list
# WebUI 自动后台运行

# 随时访问
open http://127.0.0.1:8080
```

### 场景 2: 开发调试

```bash
# 禁用自动启动
agentos webui config --no-auto-start

# 前台运行以查看日志
agentos web --reload --log-level debug
```

### 场景 3: 远程访问

```bash
# 配置监听所有接口
agentos webui config --host 0.0.0.0

# 重启生效
agentos webui restart

# 通过 IP 访问: http://your-ip:8080
```

---

## 🔧 与原有 `agentos web` 的区别

| 特性 | `agentos web` | `agentos webui start` | 自动启动 |
|------|---------------|---------------------|----------|
| 运行方式 | 前台 | 后台 | 后台 |
| 阻塞命令 | 是 | 否 | 否 |
| 查看日志 | 直接输出 | 文件 | 文件 |
| 适用场景 | 开发调试 | 生产运行 | 默认推荐 |
| 进程管理 | 手动 Ctrl+C | `webui stop` | `webui stop` |

**推荐使用**: 自动启动（日常使用）或 `agentos web`（开发调试）

---

## 📚 文档

- [WebUI 快速上手](./docs/guides/webui-quickstart.md) - 3 分钟快速体验
- [WebUI 完整指南](./docs/guides/webui.md) - 详细功能说明
- [自动启动配置](./docs/guides/webui-autostart.md) - 高级配置指南

---

## 🐛 故障排查

### 问题: WebUI 未自动启动

**解决**:
```bash
# 检查配置
agentos webui config --show

# 确认 Auto-start: Enabled
# 如未启用，执行:
agentos webui config --auto-start
```

### 问题: 端口被占用

**解决**:
```bash
# 修改端口
agentos webui config --port 8888

# 重启
agentos webui restart
```

### 问题: 无法访问

**解决**:
```bash
# 查看日志
tail -f ~/.agentos/webui.log

# 测试健康检查
curl http://127.0.0.1:8080/api/health
```

---

## 💡 技术细节

### 工作原理

1. 用户运行 `agentos` 命令
2. CLI 初始化时检查配置
3. 如果 `webui_auto_start=true`，检查 WebUI 是否运行
4. 如未运行，使用 `subprocess.Popen` 后台启动
5. 保存 PID 到 `~/.agentos/webui.pid`
6. 继续执行用户命令

### 进程管理

- **后台进程**: `start_new_session=True` 确保独立运行
- **PID 追踪**: 存储在 `webui.pid` 文件
- **健康检查**: 使用 `os.kill(pid, 0)` 检测进程
- **优雅停止**: SIGTERM → 5秒等待 → SIGKILL

---

## 🎉 享受使用！

WebUI 自动启动让 AgentOS 更加便捷。运行任何命令，WebUI 就在那里！

**立即体验**:
```bash
agentos task list
open http://127.0.0.1:8080
```

---

**AgentOS Team** | v0.3.0 | 2026-01-27
