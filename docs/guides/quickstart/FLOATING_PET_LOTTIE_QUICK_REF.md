# FloatingPet Lottie 快速参考

**版本**: v0.3.2.6 | **日期**: 2026-01-29

---

## 🚀 一分钟快速开始

### 1. 启动测试
```bash
python -m agentos.webui.app
# 访问 http://localhost:8080
# 按 Ctrl+Shift+R 清除缓存
```

### 2. 验证效果
- 查看右下角 FAB 按钮 → 小狗在 FAB 中持续动画
- 点击 FAB 打开面板 → 面板中显示相同的小狗动画
- Hover 宠物区域 → 小狗进行 3D 倾斜 ⭐ **v0.3.2.6 新增**
- 左右移动鼠标 → 小狗左右倾斜
- 上下移动鼠标 → 小狗上下倾斜
- 鼠标离开 → 小狗恢复正面
- 关闭面板，动画暂停

---

## 🎨 更换动画（3 步）

### 步骤 1: 下载动画
访问 [LottieFiles](https://lottiefiles.com/)
搜索: `kawaii cat` / `cute robot` / `chibi animal`
下载 JSON 格式

### 步骤 2: 替换文件
```bash
# 替换这个文件
agentos/webui/static/assets/lottie/pet-cute.json
```

### 步骤 3: 刷新浏览器
按 `Ctrl+Shift+R` 清除缓存

**完成！** 🎉

---

## ⭐ v0.3.2.6 新增功能

### 1. FAB 中显示小狗动画
- **原先**: Material Icon 静态图标
- **现在**: Lottie 动画小狗（与面板一致）
- **效果**: FAB 按钮中的小狗持续摇尾巴

### 2. 3D 旋转效果 ⭐⭐⭐⭐⭐
- **效果**: Hover 小狗时，小狗像立体物体一样 3D 倾斜
- **互动**:
  - 鼠标左右移动 → 小狗左右倾斜
  - 鼠标上下移动 → 小狗上下倾斜
  - 鼠标在中心 → 小狗正面
- **技术**: 使用 `perspective` + `rotateX/rotateY` 实现 3D 效果
- **恢复**: 鼠标离开后平滑恢复正面

---

## 🔧 关键文件位置

| 文件 | 路径 | 用途 |
|------|------|------|
| **动画 JSON** | `/static/assets/lottie/pet-cute.json` | 可替换 |
| **JavaScript** | `/static/js/components/FloatingPet.js` | 逻辑控制 |
| **CSS** | `/static/css/floating-pet.css` | 样式定义 |
| **HTML** | `/templates/index.html` | 引入 lottie-web |

---

## 🎯 核心代码片段

### 初始化 Lottie
```javascript
this._lottie = window.lottie.loadAnimation({
    container: document.getElementById('fp-lottie'),
    renderer: 'svg',
    loop: true,
    autoplay: false,
    path: '/static/assets/lottie/pet-cute.json',
});
```

### 播放控制
```javascript
// 打开面板时
this._lottie.play();

// 关闭面板时
this._lottie.pause();

// Hover 加速
this._lottie.setSpeed(1.3);
```

### 降级方案
```javascript
// Lottie 加载失败时显示
<span class="material-icons" style="font-size:64px;">pets</span>
```

---

## 📊 配置选项

### 自定义动画路径
```javascript
new FloatingPet({
    lottiePath: '/static/assets/lottie/my-animation.json',
    // ... 其他配置
});
```

### 调整动画尺寸
```css
.fp-lottie {
    width: 120px !important;
    height: 120px !important;
}
```

### 调整 Hover 速度
```javascript
// 在 _bindPetHover 中
this._lottie.setSpeed(1.5);  // 更快
this._lottie.setSpeed(1.0);  // 正常
```

---

## 🐛 常见问题

### Q1: 动画不显示？
**A**: 检查控制台，应该看到:
```
FloatingPet: Lottie animation loaded
```
如果没有，检查 JSON 文件路径。

### Q2: 显示的是图标而不是动画？
**A**: 这是降级方案，说明 Lottie 加载失败。
检查:
1. lottie-web CDN 是否加载
2. JSON 文件是否存在
3. JSON 格式是否正确

### Q3: 动画卡顿？
**A**: JSON 文件可能过大或过于复杂。
解决:
- 选择文件 < 100KB 的动画
- 减少动画复杂度

---

## 🎨 推荐动画关键词

在 [LottieFiles](https://lottiefiles.com/) 搜索:

**可爱风格**:
- `kawaii cat` ⭐⭐⭐⭐⭐
- `cute blob` ⭐⭐⭐⭐
- `chibi animal` ⭐⭐⭐⭐⭐
- `dancing hamster` ⭐⭐⭐
- `happy robot` ⭐⭐⭐⭐

**专业风格**:
- `ai assistant` ⭐⭐⭐
- `chatbot animation` ⭐⭐⭐⭐
- `robot helper` ⭐⭐⭐⭐

**选择建议**:
- 文件大小 < 100KB
- 循环动画
- 颜色不要太多
- 避免过于复杂

---

## ✅ 验证清单

测试时逐项检查:

### 基础功能
- [ ] Lottie 动画加载
- [ ] 打开面板时播放
- [ ] 关闭面板时暂停
- [ ] Hover 加速效果

### 降级测试
- [ ] 禁用网络，检查降级占位符
- [ ] 错误 JSON，检查降级占位符

### 其他功能
- [ ] FAB 拖拽正常
- [ ] 快捷入口正常
- [ ] 任务 Modal 正常
- [ ] 位置记忆正常

---

## 📈 性能对比

| 指标 | CSS Animation | Lottie | 评价 |
|------|--------------|--------|------|
| 文件大小 | 0 KB | ~50 KB | ✅ 可接受 |
| 内存占用 | < 1 MB | ~2-3 MB | ✅ 可接受 |
| 动画流畅度 | 60 FPS | 60 FPS | ✅ 相同 |
| 视觉效果 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ 提升 |
| 自定义性 | 低 | 高 | ✅ 提升 |

---

## 🔗 相关链接

- **Lottie 官网**: https://airbnb.io/lottie/
- **LottieFiles**: https://lottiefiles.com/
- **lottie-web GitHub**: https://github.com/airbnb/lottie-web
- **完整文档**: `FLOATING_PET_LOTTIE_INTEGRATION.md`

---

## 💡 Pro Tips

1. **选择循环动画** - 避免播放结束后静止
2. **测试 Hover 效果** - 确保加速后不会太快
3. **准备降级** - 确保 JSON 加载失败时有占位符
4. **定期更新** - 可以根据节日/活动更换动画
5. **用户反馈** - 收集用户喜好，优化动画选择

---

## 📞 支持

遇到问题？

1. 查看详细文档: `FLOATING_PET_LOTTIE_INTEGRATION.md`
2. 检查控制台日志
3. 验证文件路径
4. 测试降级方案

---

**快速参考卡片 | v0.3.2.6 | 2026-01-29**
