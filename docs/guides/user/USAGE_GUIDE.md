# 两级命令导航使用指南

## 启动方式

```bash
cd /Users/pangge/PycharmProjects/AgentOS
python -m agentos.ui.main_tui
```

## 界面说明

### 初始界面（分类列表）

```
              AgentOS
        Task Control Plane
              v0.1.0

┌─────────────────────────────────────┐
│ Type to search or select category…  │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ task        📋 Task Management       │
│ system      ⚙️ System Settings       │
│ kb          📚 Knowledge Base        │
│ memory      🧠 Memory                │
│ history     📜 History               │
└─────────────────────────────────────┘

↑↓ navigate · Enter select · Type to search
```

### 选中分类后（命令列表）

```
              AgentOS
        Task Control Plane
              v0.1.0

┌─────────────────────────────────────┐
│                                      │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ task_new     New task                │
│ task_list    List tasks              │
│ task_resume  Resume task             │
│ task_inspect Inspect task            │
└─────────────────────────────────────┘

↑↓ navigate · Enter select · ESC back
```

### 搜索模式

```
              AgentOS
        Task Control Plane
              v0.1.0

┌─────────────────────────────────────┐
│ search                               │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ kb_search    Search knowledge base   │
│ memory_search Search memories        │
└─────────────────────────────────────┘

Searching all commands · Enter select · ESC clear
```

## 操作指南

### 基础导航

| 键位 | 功能 |
|------|------|
| ↑/↓ | 上下移动选择 |
| Enter | 确认选择（选择分类或执行命令） |
| ESC | 返回上一级或清空搜索 |
| 字母/数字 | 开始搜索 |

### 使用流程

#### 方式1：分类浏览（适合探索）

1. 启动后看到5个分类
2. 使用 ↑↓ 键选择分类
3. 按 Enter 进入该分类
4. 使用 ↑↓ 键选择命令
5. 按 Enter 执行命令
6. 按 ESC 返回分类列表

#### 方式2：快速搜索（适合熟练用户）

1. 启动后直接输入关键词（如 "list"）
2. 自动显示所有匹配的命令
3. 使用 ↑↓ 键选择
4. 按 Enter 执行
5. 按 ESC 清空搜索，返回分类列表

## 分类说明

### 📋 Task Management（任务管理）
- `task_new` - 创建新任务
- `task_list` - 查看任务列表
- `task_resume` - 恢复/启动任务
- `task_inspect` - 查看任务详情

### ⚙️ System Settings（系统设置）
- `system_settings` - 配置系统
- `system_help` - 查看帮助
- `system_quit` - 退出程序

### 📚 Knowledge Base（知识库）
- `kb_search` - 搜索知识库
- `kb_refresh` - 刷新索引
- `kb_stats` - 统计信息
- 还有5个其他KB命令...

### 🧠 Memory（记忆管理）
- `memory_add` - 添加记忆
- `memory_list` - 列出记忆
- `memory_search` - 搜索记忆
- 还有6个其他Memory命令...

### 📜 History（历史记录）
- `history_list` - 查看历史
- `history_show` - 显示详情
- `history_replay` - 重放命令
- 还有4个其他History命令...

## 常见场景

### 场景1：我想查看所有任务
```
1. 启动 → 看到分类列表
2. ↓ 到 "Task Management"
3. Enter → 看到4个Task命令
4. ↓ 到 "List tasks"
5. Enter → 跳转到任务列表页面
```

### 场景2：我想搜索知识库
```
方式A（搜索）：
1. 启动 → 输入 "search"
2. 看到 kb_search 和 memory_search
3. ↓ 选择 kb_search
4. Enter → 进入搜索界面

方式B（分类）：
1. 启动 → ↓ 到 "Knowledge Base"
2. Enter → 看到8个KB命令
3. ↓ 到 "Search knowledge base"
4. Enter → 进入搜索界面
```

### 场景3：我不记得命令名了
```
1. 启动 → 输入关键词的一部分（如 "mem"）
2. 自动显示所有包含 "mem" 的命令
3. 浏览匹配结果，找到想要的命令
4. Enter 执行
```

## 提示与技巧

1. **快速返回分类**：在命令列表或搜索模式按 ESC
2. **清空搜索**：按 ESC 清空输入框
3. **全文搜索**：搜索会匹配命令的key、title、hint
4. **图标识别**：每个分类都有独特的emoji图标
5. **提示文本**：底部提示会根据当前模式动态变化

## 故障排除

### 问题：按ESC没反应
- 检查是否在分类列表模式（已经是最顶层）
- 尝试先清空输入框

### 问题：找不到某个命令
- 使用搜索模式（输入关键词）跨分类查找
- 检查命令是否已注册到系统

### 问题：命令执行没反应
- 某些命令可能需要参数（如task_inspect）
- 查看底部通知消息了解执行状态

## 技术信息

- **框架**：Textual TUI
- **响应式**：支持动态模式切换
- **兼容性**：保持向后兼容原有命令系统
- **扩展性**：可轻松添加新分类和命令

---

**版本**：v1.0
**更新日期**：2026-01-26
**维护者**：AgentOS 开发团队
