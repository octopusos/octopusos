# WebUI 快速上手指南

> **3 分钟快速体验 AgentOS WebUI**

---

## 🎯 目标

本指南将带你在 3 分钟内体验 AgentOS WebUI 的核心功能。

---

## 📦 第一步: 确认安装

确保你已安装 AgentOS：

```bash
# 检查版本
agentos --version

# 应该显示: AgentOS, version 0.3.0 (or higher)
```

如果未安装，请先安装：

```bash
pip install -e .
```

---

## 🚀 第二步: 体验自动启动

**AgentOS WebUI 默认会自动启动！** 只需运行任何命令：

```bash
# 运行任何 CLI 命令
agentos task list
```

WebUI 已在后台启动！

**验证启动**:

```bash
agentos webui status
```

你应该看到：

```
┏━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Property ┃ Value                      ┃
┡━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ Running  │ ✅ Yes                     │
│ URL      │ http://127.0.0.1:8080      │
└──────────┴────────────────────────────┘
```

---

## 🌐 第三步: 访问 WebUI

打开浏览器，访问:

**http://127.0.0.1:8080**

你应该看到 AgentOS Control Surface！

---

## 🎮 第四步: 探索功能

### 1. 查看健康状态

在右上角，你会看到一个**健康徽章** (Health Badge):

- 🟢 **OK** - 系统正常
- 🟡 **WARN** - 有警告
- 🔴 **DOWN** - 系统故障

### 2. 使用 Chat 功能

1. 左侧导航选择 **Chat**
2. 在输入框输入消息: `你好，AgentOS`
3. 点击 **Send** 或按 `Enter`
4. 观察流式消息输出

### 3. 查看 Skills

1. 左侧导航选择 **Agent → Skills**
2. 查看已加载的 Skills 列表
3. 每个 Skill 显示:
   - 名称和版本
   - 描述
   - 执行状态

### 4. 查看配置

1. 左侧导航选择 **Settings → Config**
2. 查看系统配置信息

---

## 🎛️ 第五步: 管理 WebUI

### 查看状态

```bash
agentos webui status
```

### 停止 WebUI

```bash
agentos webui stop
```

### 重启 WebUI

```bash
agentos webui restart
```

### 修改端口

```bash
# 修改为 8888 端口
agentos webui config --port 8888

# 重启生效
agentos webui restart

# 访问新地址: http://127.0.0.1:8888
```

---

## 🔧 常用配置

### 禁用自动启动

如果你不想每次运行命令都启动 WebUI：

```bash
agentos webui config --no-auto-start
```

需要时手动启动：

```bash
agentos webui start
```

### 启用自动启动

```bash
agentos webui config --auto-start
```

### 查看配置

```bash
agentos webui config --show
```

---

## 📱 访问方式对比

### 方式 1: 自动启动 (推荐)

```bash
# 运行任何命令，WebUI 自动后台启动
agentos task list

# 访问: http://127.0.0.1:8080
```

**优点**: 无需手动启动，始终可用

### 方式 2: 后台启动

```bash
# 手动后台启动
agentos webui start

# 访问: http://127.0.0.1:8080
```

**优点**: 完全控制启动时机

### 方式 3: 前台启动

```bash
# 前台运行，查看日志
agentos web --reload

# 访问: http://127.0.0.1:8080
```

**优点**: 适合开发调试

---

## 🎓 进阶使用

### 查看 WebUI 日志

```bash
# 实时查看日志
tail -f ~/.agentos/webui.log

# 查看最近 50 行
tail -n 50 ~/.agentos/webui.log
```

### 远程访问

```bash
# 配置监听所有接口
agentos webui config --host 0.0.0.0

# 重启
agentos webui restart

# 通过 IP 访问: http://your-ip:8080
```

⚠️ **安全提醒**: 仅在受信任的网络中使用 `0.0.0.0`

---

## 🐛 遇到问题？

### WebUI 无法启动

```bash
# 查看详细日志
agentos webui start --foreground
```

### 端口被占用

```bash
# 修改端口
agentos webui config --port 8888
agentos webui restart
```

### 无法访问

```bash
# 测试健康检查 API
curl http://127.0.0.1:8080/api/health
```

---

## 📚 下一步

恭喜！你已经掌握了 WebUI 的基本使用。

**推荐阅读**:

- [WebUI 完整指南](./webui.md) - 详细功能说明
- [WebUI 自动启动配置](./webui-autostart.md) - 高级配置
- [API 文档](./api.md) - REST API 和 WebSocket 协议

**开始探索**:

- 尝试创建新的 Session
- 使用 Memory 搜索功能
- 查看 Tasks 列表
- 探索不同的视图

---

**享受 AgentOS WebUI！** 🎉

如有问题，请查阅完整文档或提交 Issue。
