# Web Search Shell Extension

This extension only declares capability metadata.

To enable real execution, install internal package:

- `octopusos-ext-websearch`

And configure runtime:

- `WEB_SEARCH_EXTENSION_ENTRYPOINT=octopus_ext_websearch.plugin:create_web_search_backend`
